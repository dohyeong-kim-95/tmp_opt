# 작업 규칙

## 이 저장소가 하는 일

실제 TEST 가 비싸서 알고리즘을 실측으로 돌릴 수 없다. 그래서 **이미 확보한
관측으로 세운 반응표면**을 측정기 대역으로 두고, 그 위에서 알고리즘을 돌려본다.

목적은 성능 비교가 아니라 두 가지 검증이다:

1. 알고리즘이 프로그램적으로 완주하는가
2. 알고리즘이 근거 없는 X 를 추천하지 않는가

판정은 `python accept.py` 의 완료조건 4항목이다. 전부 시뮬레이션 장치를
참값으로 두고 표면 출력과 직접 대조하므로, 주장이 아니라 측정이다.

## 파일

| 파일 | 책임 | 있으면 안 되는 것 |
|---|---|---|
| `space.py` | 탐색 공간 표준 명세 | 문제·점수·알고리즘 |
| `calculator.py` | X → y_raw (반응표면) + obs.jsonl 입출력 | 점수·알고리즘 |
| `algo.py` | y_raw → 점수, 알고리즘 | 루프·파일 입출력 |
| `run.py` | 루프 (algo → 측정 → append) | 점수·알고리즘 |
| `make_dataset.py` | 관측 설계 + 측정 → obs.jsonl | 알고리즘·점수 |
| `accept.py` | 완료조건 판정 | production 로직 |

각 파일은 `python <file>` 로 자가 점검이 돈다.

## 지켜야 할 것

**측정 정의와 점수 정의는 각각 한 곳뿐이다.** `algo.convert_y_raw`(마스크 →
숫자 6개)와 `algo.get_scores`(숫자 → 점수 1개). 알고리즘마다 다시 구현하면
알고리즘 간 비교가 조용히 무의미해진다 — 실행은 안 깨지고 아무도 눈치채지
못한다. 밖에서 직접 루프를 돌 때도 이 함수들을 부르면 결과가 정확히 일치한다.

**silent fallback 금지.** 실패·미구현을 랜덤이나 기본값으로 조용히 대체하지
말 것. NaN/inf 는 즉시 raise, 표현 불가능한 값은 클램프하지 말고 알릴 것.
반응표면이 커버리지 밖에서 값을 지어내지 않고 `no_data` 로 표시하는 것도
같은 원칙이다.

**테스트 완화 금지.** 허용오차·임계·슬랙을 낮춰 통과시키지 말 것.

**관측 파일이 진실이다.** `obs.jsonl` 은 append-only 텍스트이고 사람이 읽고
git 으로 diff 할 수 있다. 여기에 한 줄 추가하면 반응표면에 즉시 반영된다
(완료조건 4번이 이걸 보장한다).

## 배운 것

합성 벤치마크 5종으로 optimizer 11종을 겨루던 시절의 기록은
[`lesson_learned.md`](lesson_learned.md) 에 있다. 그 코드는 `legacy` 브랜치.
